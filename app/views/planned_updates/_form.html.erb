<%= form_for @planned_update, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :execute_after, :class => 'control-label' %>
    <div class="controls">
      <%= f.datetime_select :execute_after, :class => 'datetime_select' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :page_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :page_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                planned_updates_path, :class => 'btn' %>
  </div>
<% end %>
