<%= form_for @changed_block, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :update_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :update_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :text, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :text, :class => 'text_area' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :change_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :change_type, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                changed_blocks_path, :class => 'btn' %>
  </div>
<% end %>
